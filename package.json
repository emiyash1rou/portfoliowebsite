{
  "name": "portfolio_website",
  "version": "1.0.0",
  "description": "1. Run ```git init``` to your terminal\r 2. Create a repository in github. A link will be generated for you to push. \r 3. Add The Link to the terminal by doing this ```git remote add [name] https://github.com/emiyash1rou/portfoliowebsite.git```\r 4. Check if the link is there by runnning this command ```git remote -v ```\r 5. Add a .gitignore file.\r 6. For the commands of updating github reposity, do this:\r ``` \r git add .\r git commit -m \"update\"\r git push [remote_name] master\r ```",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon app.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.1",
    "cookie-parser": "^1.4.6",
    "dotenv": "^16.0.0",
    "ejs": "^3.1.6",
    "express": "^4.17.2",
    "express-session": "^1.17.2",
    "mysql": "^2.18.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
